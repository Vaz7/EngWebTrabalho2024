extends layout

block content
    link(rel='stylesheet', href='/stylesheets/registar.css')

    div(id = "login-form-div")
        h2 Criar Conta
        form(id="login-form" action="/registar" method="post")
            p
                .input-text(title="Username")
                    i.fa-solid.fa-user.fa-lg
                    input(name="username" type="text" placeholder="Username" autofocus required)
            p
                .input-text(title="Nome")
                    i.fa-solid.fa-address-card.fa-lg
                    input(name="name" type="text" placeholder="Nome" required)
            p
                .input-text(title="Email")
                    i.fa-solid.fa-envelope.fa-lg
                    input(name="email" type="text" placeholder="Email" required)
            p
                .input-text(title="Password")
                    i.fa-solid.fa-lock.fa-lg
                    input.password(name="password" type="password" placeholder="Password" required)
            p
            button.w3-button.w3-pink.w3-round.w3-hover.w3-margin(id="formSubmit" type="submit") Criar conta
        
        div(id="message" class=(warning ? '' : 'hidden'))
            if warning
                p= warning
            else
                p         

        div(id="create-account-wrap")
            p Já possui conta? 
                a(href="/login") Login
    

    footer.w3-container.w3-pink 
        address Generated by Os Javalis::EngWeb2024 em #{d}
